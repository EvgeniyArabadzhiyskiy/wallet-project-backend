{
  "version": 2,
  "name": "wallet",

  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "headers": [{ "source": "/(.*)" }]
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}
