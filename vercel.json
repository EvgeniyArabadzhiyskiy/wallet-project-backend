{
  "version": 2,
  "headers": [{"source": "/(.*)"}],
  "name": "wallet",
  
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
  
}
